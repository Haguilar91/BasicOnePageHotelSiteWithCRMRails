# generated by dockerfile-rails
#
# Options used the last time `bin/rails generate dockerfile` ran — kept in
# sync with Dockerfile by hand. No fly_launch_runtime label: this Dockerfile
# is used for the Kamal deploy (config/deploy.yml), not Fly.io.

---
options: {}
